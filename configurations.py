import numpy as np

# edit all configurations that cannot be set inside the interactive notebook here 

# maximum number of sources to process before printing out a warning. 
Nmax = 100

# Quantiles for distance estimation: [rmed,rlo,rhi]
probs = np.array([0.5,0.1586553, 0.8413447])
